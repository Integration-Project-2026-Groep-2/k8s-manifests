apiVersion: apps/v1
kind: Deployment
metadata:
  name: facturatie
  namespace: integration-project-2026-groep-2
  labels:
    app: facturatie
  annotations:
    argocd.argoproj.io/sync-wave: "3"  # Sync after databases and infrastructure
spec:
  replicas: 1
  selector:
    matchLabels:
      app: facturatie
  template:
    metadata:
      labels:
        rabbitmq-client: "true"
        app: facturatie
    spec:
      securityContext: {}
      containers:
        - name: facturatie
          image: ghcr.io/integration-project-2026-groep-2/facturatie:latest-dev
          ports:
            - containerPort: 80
          env:
            - name: DB_HOST
              value: facturatie-db
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: BILLING_RABBITMQ_USER
            - name: RABBITMQ_PASS
              valueFrom:
                secretKeyRef:
                  name: rabbitmq-secret
                  key: BILLING_RABBITMQ_PASS
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_PASS
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: facturatie
  namespace: integration-project-2026-groep-2
  labels:
    app: facturatie
spec:
  type: ClusterIP
  selector:
    app: facturatie
  ports:
    - name: http
      port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: facturatie-db
  namespace: integration-project-2026-groep-2
  labels:
    app: facturatie-db
  annotations:
    argocd.argoproj.io/sync-wave: "2"  # Sync before applications, after infrastructure
spec:
  serviceName: facturatie-db
  replicas: 1
  selector:
    matchLabels:
      app: facturatie-db
  template:
    metadata:
      labels:
        app: facturatie-db
    spec:
      securityContext:
        runAsUser: 999
        fsGroup: 999
      containers:
        - name: facturatie-db
          image: mariadb:11.8
          ports:
            - containerPort: 3306
          env:
            - name: MARIADB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_NAME
            - name: MARIADB_USER
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_USER
            - name: MARIADB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_PASS
            - name: MARIADB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: facturatie-secret
                  key: BILLING_DB_ROOT_PASS
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            exec:
              command: ["healthcheck.sh", "--connect", "--innodb_initialized"]
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            exec:
              command: ["healthcheck.sh", "--connect"]
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: facturatie-db-data
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: facturatie-db-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: facturatie-db
  namespace: integration-project-2026-groep-2
  labels:
    app: facturatie-db
spec:
  type: ClusterIP
  clusterIP: None   # Headless for StatefulSet DNS
  selector:
    app: facturatie-db
  ports:
    - port: 3306
      targetPort: 3306
