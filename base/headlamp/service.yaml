apiVersion: v1
kind: Service
metadata:
  name: headlamp
  labels:
    app: headlamp
    app.kubernetes.io/name: headlamp
spec:
  type: ClusterIP
  selector:
    app: headlamp
    app.kubernetes.io/name: headlamp
  ports:
    - name: http
      port: 80
      targetPort: http
