apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: elasticsearch
  annotations:
    argocd.argoproj.io/sync-wave: "1"  # Ensure secrets are applied before this resource
spec:
  version: 8.19.15
  # Wire per-user basic-auth secrets into the cluster.
  auth:
    fileRealm:
      - secretName: kibana-system-basic-auth
      - secretName: svc-controlroom-basic-auth
  nodeSets:
    - name: default
      count: 1
        
      podTemplate:
        spec:
          containers:
            - name: elasticsearch
              resources:
                requests:
                  memory: 2Gi
                  cpu: 500m
                limits:
                  memory: 2Gi
                  cpu: 1000m
              # Expose ports from SealedSecret for app consumers
              env:
                - name: PORT_ELASTICSEARCH
                  valueFrom:
                    secretKeyRef:
                      name: elasticsearch-secret
                      key: PORT_ELASTICSEARCH
                - name: ES_JAVA_OPTS
                  value: "-Xms2g -Xmx2g"  # Set JVM heap size to 2Gi for resource efficiency
      volumeClaimTemplates:
        - metadata:
            name: elasticsearch-data
          spec:
            accessModes: ["ReadWriteOnce"]
            resources:
              requests:
                storage: 10Gi