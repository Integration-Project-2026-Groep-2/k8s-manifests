# Root Kustomization - Deploy all platform workloads
# This is the entry point for `kubectl apply -k .`

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply base configuration with all workloads, gateway, policies, and secrets
resources:
  - base/
