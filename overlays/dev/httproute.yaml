# Kubernetes Gateway API - Development Environment
# Dev-specific HTTPRoutes with dev subdomain routing

---
# ─── HTTPRoute: Main website (Drupal) ──────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend-route
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev.integration-project-2026-groep-2.my.be
    - dev-www.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: frontend
          port: 80

---
# ─── HTTPRoute: RabbitMQ Management UI ────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rabbitmq-route
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev-rabbitmq.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: rabbitmq
          port: 15672

---
# ─── HTTPRoute: Billing / Facturatie ──────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: billing-route
  annotations:
    nginx.gateway.networking.k8s.io/auth-type: basic
    nginx.gateway.networking.k8s.io/auth-secret: htpasswd-facturatie
    nginx.gateway.networking.k8s.io/auth-realm: "Facturatie Access (Dev)"
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev-facturatie.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: billing-app
          port: 80

---
# ─── HTTPRoute: Kassa ─────────────────────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kassa-route
  annotations:
    nginx.gateway.networking.k8s.io/auth-type: basic
    nginx.gateway.networking.k8s.io/auth-secret: htpasswd-kassa
    nginx.gateway.networking.k8s.io/auth-realm: "Kassa Access (Dev)"
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev-kassa.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: kassa
          port: 8069

---
# ─── HTTPRoute: Mailing ───────────────────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mailing-route
  annotations:
    nginx.gateway.networking.k8s.io/auth-type: basic
    nginx.gateway.networking.k8s.io/auth-secret: htpasswd-mailing
    nginx.gateway.networking.k8s.io/auth-realm: "Mailing Access (Dev)"
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev-mailing.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: mailing
          port: 3000

---
# ─── HTTPRoute: Control Room ──────────────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: controlroom-route
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev-controlroom.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: kibana
          port: 5601

---
# ─── HTTPRoute: Planning ──────────────────────────────────────────────────────
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: planning-route
spec:
  parentRefs:
    - name: main-gateway
      namespace: main-gateway
      sectionName: https
  hostnames:
    - dev-planning.integration-project-2026-groep-2.my.be
  rules:
    - backendRefs:
        - name: planning
          port: 80